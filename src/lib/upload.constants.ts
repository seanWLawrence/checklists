// TODO migrate to presigned urls upload to bypass Vercel file size limits
const MAX_IMAGE_SIZE_MB = 4.4;
const MAX_IMAGE_SIZE = `${MAX_IMAGE_SIZE_MB}mb`;
const MAX_AUDIO_SIZE_MB = 4.4;
export const MAX_AUDIO_SIZE = `${MAX_AUDIO_SIZE_MB}mb`;
